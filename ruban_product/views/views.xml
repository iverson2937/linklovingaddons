<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
        <record model="ir.ui.view" id="ruban_product.list">
          <field name="name">ruban_product list</field>
          <field name="model">ruban_product.ruban_product</field>
          <field name="arch" type="xml">
            <tree>
              <field name="name"/>
              <field name="value"/>
              <field name="value2"/>
            </tree>
          </field>
        </record>
        -->

        <!-- actions opening views on models -->
        <!--
        <record model="ir.actions.act_window" id="ruban_product.action_window">
          <field name="name">ruban_product window</field>
          <field name="res_model">ruban_product.ruban_product</field>
          <field name="view_mode">tree,form</field>
        </record>
        -->

        <!-- server action to the one above -->
        <!--
        <record model="ir.actions.server" id="ruban_product.action_server">
          <field name="name">ruban_product server</field>
          <field name="model_id" ref="model_ruban_product_ruban_product"/>
          <field name="code">
            action = {
              "type": "ir.actions.act_window",
              "view_mode": "tree,form",
              "res_model": self._name,
            }
          </field>
        </record>
        -->

        <!-- Top menu item -->
        <!--
        <menuitem name="ruban_product" id="ruban_product.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
        <menuitem name="Menu 1" id="ruban_product.menu_1" parent="ruban_product.menu_root"/>
        <menuitem name="Menu 2" id="ruban_product.menu_2" parent="ruban_product.menu_root"/>
        -->
        <!-- actions -->
        <!--
        <menuitem name="List" id="ruban_product.menu_1_list" parent="ruban_product.menu_1"
                  action="ruban_product.action_window"/>
        <menuitem name="Server to list" id="ruban_product" parent="ruban_product.menu_2"
                  action="ruban_product.action_server"/>
        -->
    </data>
</odoo>