<openerp>
    <data>
        <record id="purchase_order_tree_inherit" model="ir.ui.view">
            <field name="name">purchase.order.tree.inherit</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="linkloving_purchase.purchase_order_tree_ll"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='state']" position="after">
                    <!-- Add new fields here -->
                    <field name="status_light" widget="statuslight"/>
                </xpath>

            </field>
        </record>

        <!-- Inherit Form View to Modify it -->
        <record id="ll_purchase_order_form_inherit" model="ir.ui.view">
            <field name="name">purchase.order.form.inherit</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='partner_id']" position="after">
                    <field name="waiting_file"/>
                    <!-- Add your fields or attributes here -->
                </xpath>
                <xpath expr="//field[@name='handle_date']" position="after">
                    <field name="sale_order_handle_date"/>
                    <!-- Add your fields or attributes here -->
                </xpath>
            </field>
        </record>
    </data>
</openerp>