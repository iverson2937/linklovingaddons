<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
        <record model="ir.rule" id="sale_team_comp_rule">
            <field name="name">部分可见</field>
            <field name="model_id" ref="model_crm_lead"/>
            <field name="domain_force">[('uid','=',user.id)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        </record>

        <record model="ir.rule" id="sale_team_comp_rule">
            <field name="name">可见自己团队商机</field>
            <field name="model_id" ref="model_crm_lead"/>
            <field name="domain_force">['|',('uid.sale_team_id.user_id','=',user.id),('uid','=',user.id)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        </record>
    </data>


</odoo>
